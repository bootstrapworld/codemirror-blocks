{
  "name": "codemirror-blocks-workspace",
  "description": "Workspace for codemirror-blocks related packaged",
  "private": "true",
  "engines": {
    "node": ">=14.x",
    "npm": ">=7.x"
  },
  "workspaces": [
    "packages/cmb-toolkit",
    "packages/codemirror-blocks",
    "packages/wescheme-blocks"
  ],
  "scripts": {
    "test": "jest",
    "test-watch": "jest --watch"
  }
}