{{#> node type="expression"}}
<span class="blocks-operator">
  {{node.func}}
</span>
<span class="blocks-args">
  {{#if node.args.length}}
  {{> drop-target location=node.args.[0].from}}
  {{else}}
  {{> drop-target location=node.to}}
  {{/if}}
  {{#each node.args}}
  {{{renderNode this ../cm ../callback}}}
  {{> drop-target location=this.to}}
  {{/each}}
</span>
{{/node}}
