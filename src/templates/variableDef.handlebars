{{#> node type="variableDef"}}
<span class="blocks-operator">define</span>
<span class="blocks-args">
  <span class="blocks-name">{{node.name}}</span>
  {{{renderNode node.body cm callback}}}
</span>
{{/node}}
